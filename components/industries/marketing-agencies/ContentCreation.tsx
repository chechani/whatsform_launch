import React from 'react';
import { BenefitsGrid } from '../../../components/PageBuilder';

const ContentCreation = ({ content }: any) => {
  return <BenefitsGrid {...content} />;
};

export default ContentCreation;
